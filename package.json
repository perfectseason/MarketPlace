{
  "name": "marketplace",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "frontend": "cd frontend && bun run dev",
    "backend": "cd backend && python manage.py runserver",
    "dev": "concurrently \"bun run frontend\" \"bun run backend\""
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "concurrently": "^10.0.3"
  }
}